export interface ILCard {
  // 标题
  title?: string;
  // 圆角
  rounded?: string;
  // 阴影
  shadow?: string;
  // 上边距
  top?: number;
  // 下边距
  bottom?: number;
  // 背景色
  background?: string;
  // 自定义class
  contentClass?: string;
}
